name: Investment OS V4 财报与SEC扫描

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "只生成合并消息，不发送飞书"
        required: false
        type: boolean
        default: false
      send_test_message:
        description: "额外发送飞书连通性测试消息"
        required: false
        type: boolean
        default: false
  schedule:
    - cron: "*/30 * * * *"

concurrency:
  group: investment-os-earnings-sec
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  earnings-sec:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      - name: 配置 Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: 安装依赖并校验配置
        run: |
          pip install -r requirements.txt
          python -m src.config_validator
      - name: 恢复统一事件与决策状态
        uses: actions/cache/restore@v4
        with:
          path: state/alerts.json
          key: investment-os-shared-state-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            investment-os-shared-state-
      - name: 扫描财报、指引与SEC文件
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          SEC_USER_AGENT: ${{ vars.SEC_USER_AGENT }}
        run: >-
          python -m src.main --mode earnings
          ${{ inputs.dry_run && '--dry-run' || '' }}
          ${{ inputs.send_test_message && '--test-message' || '' }}
      - name: 保存统一事件与决策状态
        if: always() && hashFiles('state/alerts.json') != ''
        uses: actions/cache/save@v4
        with:
          path: state/alerts.json
          key: investment-os-shared-state-${{ github.run_id }}-${{ github.run_attempt }}
